const Employee = require('../lib/Employee.js')

it ('Should create an instance of class Employee', () => {
   const newEmployee = new Employee employee() // arange, act, and assert
   expect(typeOf newEmployee.toBe('object')
})

// test or it can be used as statement keyword
// 1 npm init
// 2 node i jest